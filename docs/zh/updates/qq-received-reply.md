# QQ 收到消息后的反馈说明

## 当前行为

QQ 通道收到用户私聊消息后，**不再**先发一条“收到，正在处理～”类文案；消息直接进入 Agent 处理流程。

若需要“正在处理”的视觉反馈，可使用 QQ 机器人平台自带的「正在输入」或回复 `...` 等能力，由平台或业务侧自行实现。

## 配置

`channels.qq` 中已移除 `received_reply` 配置项；保留 `enabled`、`app_id`、`secret`、`allow_from` 等。
